const renderIndex = (req, res) => {
    res.render('index');
};

const renderNewEntry = (req, res) => {};

const createNewEntry = (req, res) => {};

module.exports = {
    renderIndex,
    renderNewEntry,
    createNewEntry
};
